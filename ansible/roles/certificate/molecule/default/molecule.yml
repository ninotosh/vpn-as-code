---
dependency:
  name: galaxy
  options:
    force: false
driver:
  name: docker
platforms:
  - name: instance
    image: ubuntu:24.04 # renovate
provisioner:
  env:
    ANSIBLE_ROLES_PATH: ../../../ # https://github.com/ansible/molecule/issues/4391
